export const STUDY_NOTE_FORMAT_PROMPT = `請把接下來的內容轉成「網站筆記可以完整還原的 Markdown」。

目標不是重寫，也不是硬套模板；目標是讓我可以直接複製貼上到筆記系統，排版不要跑掉。

請遵守：
1. 保留原本內容的邏輯、順序和語氣，不要自行補不存在的段落。
2. 用 Markdown 標題呈現大字到小字的層級，例如 #、##、###、####。如果原文沒有那麼多層，就不要硬補。
3. 保留表格，但請輸出成標準 Markdown table，讓網站能正確顯示。
4. 保留條列、編號、粗體、引用提醒、code block。
5. 不要輸出 HTML，不要輸出圖片連結，不要包成 JSON。
6. 如果原文有題號，而且你確定是網站題庫的題目，可以額外用這個短碼獨立放一行：
[question-note id="題目ID" title="簡短題目標題"]
7. 如果沒有明確題號，不要硬產生 question-note。
8. 只輸出 Markdown 正文，不要加「以下是整理後內容」這種開場白。

簡單說：請把你的原本好讀排版，轉成乾淨、標準、可貼進網站的 Markdown。`;
